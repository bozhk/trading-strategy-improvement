"use strict";
const $=(id)=>document.getElementById(id);const esc=(v)=>String(v??"").replace(/[&<>"']/g,(c)=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
const num=(v,d=2)=>Number(v||0).toLocaleString("en-US",{minimumFractionDigits:d,maximumFractionDigits:d});const money=(v)=>`${Number(v||0)<0?"-":""}$${num(Math.abs(Number(v||0)),2)}`;const price=(v)=>Number(v||0).toLocaleString("en-US",{maximumFractionDigits:Number(v)<1?6:2});
const duration=(s)=>{s=Math.max(0,Math.floor(Number(s)||0));return [Math.floor(s/3600),Math.floor(s%3600/60),s%60].map(v=>String(v).padStart(2,"0")).join(":")};const time=(ts)=>ts?new Date(ts*1000).toLocaleTimeString("en-GB",{hour:"2-digit",minute:"2-digit",second:"2-digit",timeZone:"UTC"}):"—";
function tone(el,v){el.classList.remove("positive","negative");if(Number(v)>0)el.classList.add("positive");if(Number(v)<0)el.classList.add("negative")}
function render(s){const st=s.stats||{};$("pnl").textContent=money(st.pnl);tone($("pnl"),st.pnl);$("pnl-note").textContent=`Realized ${money(st.realized)} · Open ${money(st.unrealized)}`;$("win-rate").textContent=`${num(st.win_rate,1)}%`;$("record").textContent=`${st.wins||0}W / ${st.losses||0}L`;$("profit-factor").textContent=Number.isFinite(Number(st.profit_factor))?num(st.profit_factor):"∞";$("trades").textContent=`${st.trades||0} closed trades`;$("open-count").textContent=String(st.open||0).padStart(2,"0");$("uptime").textContent=`Uptime ${duration(s.uptime)}`;$("session").textContent=String(s.session||"—").toUpperCase();$("mode").textContent=String(s.session||"PAPER").toUpperCase();$("source-badge").textContent=`SOURCE ${String(s.source||"—").toUpperCase()}`;$("last-update").textContent=`SYNC ${time(s.timestamp)} UTC`;
const radar=Array.isArray(s.radar)?s.radar:[];$("radar-body").innerHTML=radar.length?radar.slice(0,20).map(r=>{const flow=Number(r.imbalance??r.delta??r.flow??0);const signal=r.signal||r.status||"MONITOR";return `<tr><td class="symbol">${esc(r.symbol)}</td><td>${price(r.price??r.mid)}</td><td>${num(r.spread_pct??r.spread,3)}%</td><td class="${flow>0?"positive":flow<0?"negative":""}">${flow>0?"+":""}${num(flow,2)}</td><td><span class="signal">${esc(signal)}</span></td></tr>`}).join(""):'<tr><td colspan="5" class="empty">No instruments received yet. Scanner is initializing.</td></tr>';
const positions=Array.isArray(s.positions)?s.positions:[];$("position-count").textContent=String(positions.length).padStart(2,"0");$("positions").innerHTML=positions.length?positions.map(p=>`<article class="position"><div><span>INSTRUMENT</span><b class="symbol">${esc(p.symbol)} · <i class="side ${p.side==="LONG"?"positive":"negative"}">${esc(p.side)}</i></b></div><div><span>ENTRY</span><b>${price(p.entry)}</b></div><div><span>MARK</span><b>${price(p.mark)}</b></div><div><span>STOP / TARGET</span><b>${price(p.stop_price)} / ${price(p.target_price)}</b></div><div><span>UNREALIZED</span><b class="${Number(p.unrealized)>=0?"positive":"negative"}">${money(p.unrealized)}</b></div></article>`).join(""):'<div class="empty-block">No open positions. The engine is scanning for qualified setups.</div>';
const logs=Array.isArray(s.logs)?s.logs:[];$("event-count").textContent=String(logs.length).padStart(2,"0");$("logs").innerHTML=logs.length?logs.slice(0,30).map(l=>`<article class="event ${esc(String(l.level||"").toLowerCase())}"><div class="event-head"><span>${esc(l.symbol||"SYSTEM")}</span><time>${time(l.timestamp)} UTC</time></div><p>${esc(l.message)}</p></article>`).join(""):'<div class="empty-block">No events yet. Engine telemetry will appear here.</div>';
const trades=Array.isArray(s.closed_trades)?s.closed_trades:[];$("trades-body").innerHTML=trades.length?trades.slice(0,20).map(t=>`<tr><td>${time(t.closed_at)} UTC</td><td class="symbol">${esc(t.symbol)}</td><td class="side ${t.side==="LONG"?"positive":"negative"}">${esc(t.side)}</td><td>${price(t.entry)} → ${price(t.exit)}</td><td class="${Number(t.pnl)>=0?"positive":"negative"}">${money(t.pnl)}</td><td>${esc(t.reason)}</td></tr>`).join(""):'<tr><td colspan="6" class="empty">No closed trades in this session.</td></tr>';
$("error-banner").hidden=true;$("status-dot").className="status-dot online";$("connection-label").textContent="Telemetry online"}
async function refresh(){try{const r=await fetch("/api/snapshot",{cache:"no-store"});if(!r.ok)throw new Error(`HTTP ${r.status}`);render(await r.json())}catch(_){$("error-banner").hidden=false;$("status-dot").className="status-dot offline";$("connection-label").textContent="Telemetry offline"}}
function tick(){$("clock").textContent=`${new Date().toLocaleTimeString("en-GB",{hour12:false,timeZone:"UTC"})} UTC`}tick();refresh();setInterval(tick,1000);setInterval(refresh,1000);
